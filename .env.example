# Database
POSTGRES_USER=thecoach
POSTGRES_PASSWORD=thecoach_dev
POSTGRES_DB=thecoach
DATABASE_URL=postgresql+asyncpg://thecoach:thecoach_dev@db:5432/thecoach

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8002

# All other credentials (Garmin, Withings, Anthropic API key) are
# managed in the Settings page and stored in the database.
# You can still set them here as fallback:
# GARMIN_EMAIL=
# GARMIN_PASSWORD=
# WITHINGS_CLIENT_ID=
# WITHINGS_CLIENT_SECRET=
# ANTHROPIC_API_KEY=
