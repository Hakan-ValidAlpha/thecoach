# Database
POSTGRES_USER=thecoach
POSTGRES_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD
POSTGRES_DB=thecoach
DATABASE_URL=postgresql+asyncpg://thecoach:CHANGE_ME_TO_A_STRONG_PASSWORD@db:5432/thecoach

# Frontend API URL — leave empty for relative URLs (works when Caddy proxies both)
NEXT_PUBLIC_API_URL=

# Optional: set credentials here or manage in the Settings page
# GARMIN_EMAIL=
# GARMIN_PASSWORD=
# ANTHROPIC_API_KEY=
